DEF NEW GLOBAL SHARED VAR pick-value AS CHAR NO-UNDO.

DEF {1} SHARED VAR buf-copy-doc AS CHAR NO-UNDO. /* для создания записи msg-impexp*/
DEF NEW SHARED VAR ref-920 AS CHAR NO-UNDO.

DEF VAR buf-sum       AS dec   NO-UNDO.
DEF VAR choice        AS log   NO-UNDO.
DEF VAR cur-op-date   LIKE op.op-date NO-UNDO.
DEF VAR debug         AS INT64   NO-UNDO.
DEF VAR flag-err      AS log   NO-UNDO.
DEF VAR flag-go       AS INT64   NO-UNDO.
DEF VAR flagfirst950  AS log   NO-UNDO.
DEF VAR in-type       AS CHAR  NO-UNDO.
DEF VAR id-bank       AS INT64   NO-UNDO.
DEF VAR inn-pl        AS CHAR  NO-UNDO FORMAT "999999999999".
DEF VAR inn-org-pl    AS CHAR  NO-UNDO FORMAT "999999999999".
DEF VAR id-rel-mess   AS CHAR  NO-UNDO.
DEF VAR in-user-num   LIKE mail-user.mail-user-num NO-UNDO.
DEF VAR in-series-num LIKE op-impexp.exp-batch     NO-UNDO.
DEF VAR j             AS INT64   NO-UNDO.
DEF VAR mTmpStr       AS CHAR  NO-UNDO.
DEF VAR mTmpStr2      AS CHAR  NO-UNDO.
DEF VAR nd            AS INT64   NO-UNDO.
DEF VAR nn            AS INT64   NO-UNDO.
DEF VAR ndd           AS INT64   NO-UNDO.
DEF VAR name          AS CHAR  NO-UNDO EXTENT 3.
DEF VAR name-pl       AS CHAR  NO-UNDO.
DEF VAR preview-c     AS CHAR  NO-UNDO.
DEF VAR rid           AS recid NO-UNDO.
DEF VAR str-bank      AS CHAR  NO-UNDO.
DEF VAR str-acct      AS CHAR  NO-UNDO.
DEF VAR status-before AS CHAR  NO-UNDO.
DEF VAR status-done   AS CHAR  NO-UNDO.  
DEF VAR status-err    AS CHAR  NO-UNDO.
DEF VAR status-300    AS CHAR  NO-UNDO.  
DEF VAR status-fu     AS CHAR  NO-UNDO.  
DEF VAR test-ser      AS CHAR  NO-UNDO.
DEF VAR test          AS log   NO-UNDO INIT NO.
DEF VAR temp-inn      AS CHAR  NO-UNDO.     
DEF VAR vstr          AS CHAR  NO-UNDO INITIAL "link-op-sum".
DEF VAR zop           AS CHAR  NO-UNDO.
DEF VAR vMsg          AS CHAR NO-UNDO.
DEFINE VARIABLE vDigital LIKE doc-type.digital NO-UNDO.

DEF STREAM err.

DEF BUFFER kacct      FOR acct.
DEF BUFFER xop        FOR op.
DEF BUFFER xop-entry  FOR op-entry.
DEF BUFFER xop-bank   FOR op-bank.
DEF BUFFER xop-impexp FOR op-impexp.
DEF BUFFER xseries    FOR series.
DEF BUFFER zmsg-impexp FOR msg-impexp.
DEF BUFFER zop-entry   FOR op-entry.

{sw-msg.cp}
{intrface.get xclass}
{intrface.get op}
/* Замена Плюс банк
{intrface.get swi} */
{intrface.get swi_n}
/* Конец замены Плюс банк */
{intrface.get import}
{intrface.get count}

