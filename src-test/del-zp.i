        for each w-op:        delete w-op.       end.
        for each w-op-entry:  delete w-op-entry. end.
        for each w-op-bank:   delete w-op-bank.  end.
        for each w-signs:     delete w-signs.  end.
