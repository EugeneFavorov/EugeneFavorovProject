/* å•‚Æ§ Look ≠† Ñê opb: í‡†≠ßäá */

def input param in-class as char no-undo.
def input param in-optr  as char no-undo.
def input param level    as INT64  no-undo.

run VALUE("op(tr.p") (INT64(in-optr),level).
