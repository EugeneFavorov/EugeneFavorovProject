/*
               Банковская интегрированная система БИСквит
    Copyright: (C) 1992-2010 ЗАО "Банковские информационные системы"
     Filename: cfobrowse.BUP
      Comment: ЦФО - проверка перед редактированием
   Parameters:
         Uses:
      Used by:
      Created: ayv
     Modified:
*/

IF       mNavLastKey EQ KEYCODE ("INS") THEN 
DO:
   IF NOT GetCodePermission("", in-class, "c") THEN 
      NEXT ACTION.
END.
ELSE IF NOT GetCodePermission("", in-class, "w") THEN 
   NEXT ACTION.
